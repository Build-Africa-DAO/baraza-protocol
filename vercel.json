{
  "buildCommand": "npm --prefix 'Project Title' install --legacy-peer-deps && npm --prefix 'Project Title' run build",
  "outputDirectory": "Project Title/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
